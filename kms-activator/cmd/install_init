#!/bin/bash
# 安装前：停止已有实例

if [ -f "${TRIM_PKGVAR}/vlmcsd.pid" ]; then
    pid=$(head -n 1 "${TRIM_PKGVAR}/vlmcsd.pid" | tr -d '[:space:]')
    if [ -n "$pid" ] && kill -0 "$pid" 2>/dev/null; then
        kill -TERM "$pid" 2>/dev/null
        sleep 1
    fi
fi
exit 0
