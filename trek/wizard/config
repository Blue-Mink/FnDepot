[
  {
    "stepTitle": "端口配置",
    "items": [
      {
        "type": "text",
        "field": "wizard_access_port",
        "label": "访问端口",
        "initValue": "32679",
        "rules": [
          {
            "required": true,
            "message": "请输入访问端口"
          },
          {
            "pattern": "^([1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$",
            "message": "端口号必须为 1-65535 之间的数字"
          }
        ],
        "helpText": "修改 TREK 外部访问端口，修改后需要重启应用生效"
      },
      {
        "type": "tips",
        "helpText": "修改端口后，Docker 容器端口映射将同步更新，确保内外端口一致。"
      }
    ]
  }
]
