#!/bin/bash
set -euo pipefail
. "$(dirname "$0")/common"
case "${1:-}" in
  start) start_service ;;
  stop) stop_service ;;
  status) pid_running && exit 0 || exit 3 ;;
  *) write_temp_log "Usage: $0 {start|stop|status}"; exit 1 ;;
esac
