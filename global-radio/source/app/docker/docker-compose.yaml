services:
  global-radio:
    image: superneed/global-radio:latest
    container_name: global-radio
    restart: unless-stopped
    ports:
      - "${wizard_access_port:-32678}:80"
    volumes:
      - "${TRIM_PKGVAR:-/vol1/@appdata/global-radio}/data:/app/data"
